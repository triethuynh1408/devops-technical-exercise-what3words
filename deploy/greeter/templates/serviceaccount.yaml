apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "greeter.fullname" . }}
  labels:
    {{- include "greeter.labels" . | nindent 4 }}
# The service never talks to the Kubernetes API, so it gets its own identity
# with no token mounted rather than inheriting the namespace's "default" SA.
automountServiceAccountToken: false
