# The dev environment. Only what differs from the chart defaults.
greeting_name = "dev team"
replica_count = 2
image_tag     = "dev"
node_port     = 30080 # host localhost:8080 (cluster/kind-config.yaml)
